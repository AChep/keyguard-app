<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".benchmark.BenchmarkScreenRecordingReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="com.artemchep.keyguard.benchmark.ENABLE_SCREEN_RECORDING" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
