package com.artemchep.keyguard.common.service.placeholder

/** Factories in placeholder matching order; the first matching factory wins. */
class PlaceholderFactoryRegistry(val values: List<Placeholder.Factory>)
