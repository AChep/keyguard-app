[package]
name = "common-gpg-agent-rust"
version = "0.1.0"
edition = "2021"
description = "Shared Rust utilities for Keyguard GPG agent binaries"

[dependencies]
prost = "0.13"

[build-dependencies]
prost-build = "0.13"
