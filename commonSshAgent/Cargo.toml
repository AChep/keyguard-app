[package]
name = "common-ssh-agent-rust"
version = "0.1.0"
edition = "2021"
rust-version = "1.94.0"
description = "Shared Rust utilities for Keyguard SSH agent binaries"
license-file = "../LICENSE"
repository = "https://github.com/AChep/keyguard-app"
publish = false

[dependencies]
prost = "0.13"

[build-dependencies]
prost-build = "0.13"
