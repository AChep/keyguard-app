[package]
name = "keyguard-lib"
version = "0.1.0"
edition = "2021"
description = "Native desktop helper library for Keyguard"

[lib]
name = "keyguard"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"

[profile.release]
strip = true
lto = true
codegen-units = 1
