---
title: Large type
description: Show a password or any field value in big, individually numbered characters — easy to read aloud or type on another screen.
category: reference
order: 9
---

**Large type** displays a field value in large, distinct characters so you
can read a secret aloud or type it into another device — such as a phone, a TV,
or a console.

## Show a value as a large type

Pick **Show in Large Type** on a field or value. It's available on passwords, URLs, 
send values, generated values from the [generator](/docs/generator/) history, 
and account fields — anywhere a field menu appears.

On mobile, **Show in Large Type and Lock vault** displays the value and locks
the vault at the same time — useful when handing your phone to someone to type
a password.

Large type only offers itself for values up to **128 characters**.

### How it works

- Each character is shown large and on its own, **numbered in sequence**.
- **Tap a character** to mark your position — that character and every one
  before it is highlighted, so you can track how far you've read.
- When the value supports it, characters are **colour-coded by class**: digits
  in one colour, symbols in another, and letters in the default colour.
- The screen **stays awake** while large type is open.
