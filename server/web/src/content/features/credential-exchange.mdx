---
title: "Credential exchange"
summary: "Move passwords, passkeys and more directly between password managers."
icon: "🔁"
group: "Your data"
order: 12
featured: false
premium: false
---

On Android, Keyguard implements the FIDO Alliance's Credential Exchange Format, so logins, passkeys, one-time passwords, cards, identities, notes, SSH keys and folders can move straight between Keyguard and another password manager on the same device.

The [credential exchange documentation](/docs/credential-exchange/) lists the requirements and which data types make the trip. To export into a local file instead, see [data export](/features/data-export/).
