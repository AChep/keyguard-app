# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.56"
name = "polyval"
version = "0.6.2"
autobenches = false
authors = ["RustCrypto Developers"]
description = """
POLYVAL is a GHASH-like universal hash over GF(2^128) useful for constructing
a Message Authentication Code (MAC)
"""
documentation = "https://docs.rs/polyval"
readme = "README.md"
keywords = [
    "aes-gcm",
    "aes-gcm-siv",
    "crypto",
    "ghash",
    "universal-hashing",
]
categories = [
    "cryptography",
    "no-std",
]
license = "Apache-2.0 OR MIT"
repository = "https://github.com/RustCrypto/universal-hashes"

[package.metadata.keyguard-vendor]
reviewed = true
upstream-checksum = "9d1fe60d06143b2430aa532c94cfe9e29783047f06c0d7fd359a9a51b729fa25"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[dependencies.cfg-if]
version = "1"

[dependencies.opaque-debug]
version = "0.3"

[dependencies.universal-hash]
version = "0.5"
default-features = false

[dependencies.zeroize]
version = "=1.9.0"
optional = true
default-features = false

[dev-dependencies.hex-literal]
version = "0.4.1"

[features]
std = ["universal-hash/std"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(polyval_armv8)", "cfg(polyval_force_soft)"] }

[target."cfg(any(target_arch = \"aarch64\", target_arch = \"x86_64\", target_arch = \"x86\"))".dependencies.cpufeatures]
version = "0.2"
